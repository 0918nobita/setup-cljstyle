{
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "spago bundle --minify --platform node --module SetupCljstyle.Main --outfile setup-cljstyle.js",
    "lint": "purs-tidy check src test",
    "format": "purs-tidy format-in-place src test",
    "test": "spago test"
  },
  "dependencies": {
    "@actions/io": "^1.1.1",
    "@actions/tool-cache": "^1.7.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.11",
    "purescript": "^0.15.14",
    "purs-tidy": "^0.10.1",
    "spago": "^0.93.22"
  }
}
