{
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel"
  },
  "turbo": {
    "baseBranch": "origin/main",
    "pipeline": {
      "build": {
        "dependsOn": [
          "^build"
        ]
      },
      "dev": {
        "cache": false
      }
    }
  },
  "devDependencies": {
    "eslint": "^8.5.0",
    "turbo": "^1.0.19",
    "typescript": "^4.5.4"
  }
}
