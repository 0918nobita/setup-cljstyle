name: Test Action

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          cljstyle-version: 0.13.0
      - run: ls -la
      - run: cljstyle version
